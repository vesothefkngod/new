<!DOCTYPE html>
<html lang="bg">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : 'Dark Crypto Store' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css" />
  <script defer src="/js/app.js"></script>
</head>
<body class="bg">
<header class="container">
  <div class="nav">
    <a class="brand" href="/">DARK<span>CRYPTO</span></a>
    <nav class="navlinks">
      <a href="/#store">Магазин</a>
      <a href="/products">Продукти</a>
      <a href="/cart">Количка</a>
      <% if (!user) { %>
        <a href="/auth/login">Вход</a>
        <a class="btn" href="/auth/register">Регистрация</a>
      <% } else { %>
        <span class="muted">Здравей, <%= user.username %></span>
        <form method="POST" action="/auth/logout" style="display:inline">
          <button class="btn outline">Изход</button>
        </form>
      <% } %>
    </nav>
  </div>
</header>
<main class="container">
